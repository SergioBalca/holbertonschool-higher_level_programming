-- script that computes the score average of all records in second_table of database hbtn_0c_0 in MySQL server
-- The result column name is average
-- The database name is passed as argument of mysql command
SELECT AVG(score) AS average
FROM second_table;
