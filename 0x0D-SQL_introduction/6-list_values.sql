-- script that lists all rows of the first table from database hbtn_0c_0 in MySQL server
-- with all the filds printed
-- the database name will be passed as an argument of the mysql command
SELECT * FROM first_table;
